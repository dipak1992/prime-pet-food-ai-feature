<section class="pch-hero" aria-label="{{ collection.title }} collection">
  <div class="pch-hero__inner">

    <div class="pch-shell pch-hero__content">
      <div class="pch-hero__text">
        <p class="pch-kicker">{{ section.settings.eyebrow | default: 'All Prime Pet Food chews' }}</p>
        <h1>{{ section.settings.heading | default: collection.title }}</h1>
        {% if section.settings.subheading != blank %}
          <p class="pch-hero__sub">{{ section.settings.subheading }}</p>
        {% elsif collection.description != blank %}
          <p class="pch-hero__sub">{{ collection.description | strip_html | truncate: 180 }}</p>
        {% endif %}
        <div class="pch-hero__pills" aria-label="Key benefits">
          <span>
            {% render 'prime-blueprint-icon', name: 'safety', size: 13 %}
            Rawhide-free
          </span>
          <span>
            {% render 'prime-blueprint-icon', name: 'chew-time', size: 13 %}
            Classic, flavour, bulk, sizes
          </span>
          <span>
            {% render 'prime-blueprint-icon', name: 'ingredients', size: 13 %}
            Choose by dog fit
          </span>
          <span>
            {% render 'prime-blueprint-icon', name: 'love', size: 13 %}
            {{ collection.all_products_count }} product{% if collection.all_products_count != 1 %}s{% endif %} to compare
          </span>
        </div>
        <div class="pch-size-nav" aria-label="Shop by dog size">
          <a href="#product-grid" data-size-filter="Small">Small<br><span>1–15 lbs</span></a>
          <a href="#product-grid" data-size-filter="Medium">Medium<br><span>15–35 lbs</span></a>
          <a href="#product-grid" data-size-filter="Large">Large<br><span>35–55 lbs</span></a>
          <a href="#product-grid" data-size-filter="Extra Large">XL<br><span>55–85 lbs</span></a>
          <a href="#product-grid" data-size-filter="Jumbo">Jumbo<br><span>85+ lbs</span></a>
        </div>

        <p class="pch-product-count" role="status" aria-live="polite">
          {%- if collection.products_count < collection.all_products_count -%}
            Showing <strong>{{ collection.products_count }}</strong> of <strong>{{ collection.all_products_count }}</strong> products
            &nbsp;&mdash;&nbsp;<a href="{{ collection.url }}" class="pch-product-count__clear">Clear filters</a>
          {%- else -%}
            <strong>{{ collection.all_products_count }}</strong> product{% if collection.all_products_count != 1 %}s{% endif %} available
          {%- endif -%}
        </p>
      </div>

      {% if collection.products.size > 0 %}
        <div class="pch-product-panel" aria-label="Products in this collection">
          <div class="pch-product-panel__head">
            <span>Pick the right starting point</span>
            <strong>All four options are here.</strong>
          </div>

          <div class="pch-product-panel__grid">
            {% for product in collection.products limit: 4 %}
              {% liquid
                assign product_text = product.title | append: ' ' | append: product.handle | append: ' ' | append: product.tags | downcase
                assign product_badge = 'Everyday chew'
                assign product_note = 'Best when you want a simple, rawhide-free chew for supervised calm-time.'

                if product_text contains 'bulk' or product_text contains '2-lb' or product_text contains '2 lb' or product_text contains 'multi-pack' or product_text contains 'multipack'
                  assign product_badge = 'Bulk restock'
                  assign product_note = 'For dogs who already love yak chews, stronger routines, and multi-dog homes.'
                elsif product_text contains 'flavour' or product_text contains 'flavor' or product_text contains 'flavoured' or product_text contains 'flavored' or product_text contains 'peanut' or product_text contains 'bacon' or product_text contains 'cheese flavor'
                  assign product_badge = 'More tempting'
                  assign product_note = 'For picky dogs or first-time chewers who need a little more reason to start.'
                elsif product_text contains 'jumbo' or product_text contains 'large' or product_text contains 'xl'
                  assign product_badge = 'Bigger dogs'
                  assign product_note = 'For larger mouths and stronger chewers who need the right size first.'
                endif
              %}

              <a href="{{ product.url }}" class="pch-product-card">
                <div class="pch-product-card__media">
                  {% if product.featured_image %}
                    <img
                      src="{{ product.featured_image | image_url: width: 360 }}"
                      alt="{{ product.featured_image.alt | default: product.title | escape }}"
                      width="{{ product.featured_image.width }}"
                      height="{{ product.featured_image.height }}"
                      loading="{% if forloop.index <= 2 %}eager{% else %}lazy{% endif %}"
                    >
                  {% endif %}
                </div>
                <div class="pch-product-card__body">
                  <span>{{ product_badge }}</span>
                  <strong>{{ product.title }}</strong>
                  <p>{{ product_note }}</p>
                  <small>From {{ product.price | money }}</small>
                </div>
              </a>
            {% endfor %}
          </div>
        </div>
      {% endif %}
    </div>

  </div>

  <div class="pch-trust-bar" aria-label="Trust signals">
    <div class="pch-shell pch-trust-bar__inner">
      <div class="pch-trust-bar__item">
        {% render 'prime-blueprint-icon', name: 'ingredients', size: 18 %}
        <span>Simple ingredient promise</span>
      </div>
      <div class="pch-trust-bar__divider" aria-hidden="true"></div>
      <div class="pch-trust-bar__item">
        {% render 'prime-blueprint-icon', name: 'review', size: 18 %}
        <span>4.9&#9733; from 2,400+ reviews</span>
      </div>
      <div class="pch-trust-bar__divider" aria-hidden="true"></div>
      <div class="pch-trust-bar__item">
        {% render 'prime-blueprint-icon', name: 'shipping', size: 18 %}
        <span>Free shipping over $30</span>
      </div>
      <div class="pch-trust-bar__divider" aria-hidden="true"></div>
      <div class="pch-trust-bar__item">
        {% render 'prime-blueprint-icon', name: 'guarantee', size: 18 %}
        <span>30-day satisfaction guarantee</span>
      </div>
    </div>
  </div>
</section>

<style>
  /* ═══════════════════════════════════════════════════════════════
     PRIME COLLECTION HERO
  ════════════════════════════════════════════════════════════════ */
  .pch-hero {
    --pch-ink: #171512;
    --pch-cream: #fffaf2;
    --pch-clay: #b85f32;
    --pch-clay-dark: #94451f;
    --pch-muted: #5f574e;
    --pch-line: rgba(55, 43, 31, 0.13);
    font-family: var(--prime-font-sans, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif);
  }

  /* Product count indicator */
  .pch-product-count {
    margin: 12px 0 0;
    font-size: 13px;
    color: #5f574e;
    line-height: 1.4;
  }

  .pch-product-count strong {
    color: #171512;
    font-weight: 700;
  }

  .pch-product-count__clear {
    color: #b85f32;
    text-decoration: underline;
    text-underline-offset: 2px;
    font-weight: 600;
    transition: color 0.15s ease;
  }

  .pch-product-count__clear:hover {
    color: #94451f;
  }

  .pch-switch {
    background: #fff;
    border-bottom: 1px solid rgba(55, 43, 31, 0.12);
    padding: 22px 0;
    font-family: var(--prime-font-sans, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif);
  }

  .pch-hero * { box-sizing: border-box; }

  .pch-shell {
    width: min(1160px, calc(100% - 32px));
    margin: 0 auto;
  }

  /* ═══════════════════════════════════════════════════════════════
     HERO LAYOUT
  ════════════════════════════════════════════════════════════════ */
  .pch-hero__inner {
    position: relative;
    min-height: 0;
    display: grid;
    overflow: hidden;
    isolation: isolate;
    background:
      linear-gradient(135deg, rgba(184, 95, 50, 0.08), rgba(255, 250, 242, 0) 48%),
      var(--pch-cream);
    border-bottom: 1px solid var(--pch-line);
  }

  .pch-hero__content {
    padding: 42px 0 34px;
    display: grid;
    grid-template-columns: minmax(0, 1.05fr) minmax(360px, 0.95fr);
    gap: 34px;
    align-items: center;
  }

  /* ═══════════════════════════════════════════════════════════════
     HERO TEXT
  ════════════════════════════════════════════════════════════════ */
  .pch-kicker {
    margin: 0 0 10px;
    color: var(--pch-clay, #b85f32);
    font-size: 11px;
    font-weight: 780;
    letter-spacing: var(--prime-kicker-letter-spacing, 0.08em);
    text-transform: uppercase;
  }

  .pch-hero__text h1 {
    margin: 0;
    color: var(--pch-ink, #171512);
    font-family: var(--prime-font-display, var(--prime-font-sans, inherit));
    font-size: clamp(32px, 4.2vw, 52px);
    font-weight: 760;
    line-height: 1.04;
    letter-spacing: 0;
    max-width: 760px;
  }

  .pch-hero__sub {
    margin: 14px 0 0;
    color: var(--pch-muted, #5f574e);
    font-size: 16px;
    line-height: 1.6;
    max-width: 660px;
  }

  .pch-hero__pills {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 20px;
  }

  .pch-size-nav {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 8px;
    margin-top: 22px;
    max-width: 760px;
  }

  .pch-size-nav a {
    min-height: 54px;
    padding: 10px 8px;
    border: 1px solid rgba(55, 43, 31, 0.12);
    border-radius: 8px;
    background: #fff;
    color: var(--pch-ink, #171512);
    text-align: center;
    text-decoration: none;
    font-size: 13px;
    font-weight: 760;
    line-height: 1.15;
    transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease;
  }

  .pch-size-nav a:hover {
    border-color: rgba(184, 95, 50, 0.45);
    box-shadow: 0 10px 28px rgba(23, 59, 43, 0.08);
    transform: translateY(-1px);
  }

  .pch-size-nav span {
    color: var(--pch-muted, #5f574e);
    font-size: 11px;
    font-weight: 620;
  }

  .pch-product-panel {
    align-self: stretch;
    display: grid;
    gap: 12px;
    min-width: 0;
  }

  .pch-product-panel__head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    color: var(--pch-muted, #5f574e);
    font-size: 12px;
    line-height: 1.3;
  }

  .pch-product-panel__head span {
    color: var(--pch-clay, #b85f32);
    font-weight: 820;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .pch-product-panel__head strong {
    color: var(--pch-ink, #171512);
    font-weight: 760;
  }

  .pch-product-panel__grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
  }

  .pch-product-card {
    display: grid;
    grid-template-columns: 104px minmax(0, 1fr);
    min-height: 148px;
    overflow: hidden;
    border: 1px solid rgba(55, 43, 31, 0.12);
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.86);
    color: var(--pch-ink, #171512);
    text-decoration: none;
    box-shadow: 0 16px 36px rgba(23, 21, 18, 0.06);
    transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease;
  }

  .pch-product-card:hover {
    border-color: rgba(184, 95, 50, 0.36);
    box-shadow: 0 18px 42px rgba(23, 21, 18, 0.1);
    transform: translateY(-1px);
  }

  .pch-product-card__media {
    display: grid;
    place-items: center;
    min-width: 0;
    background:
      radial-gradient(circle at 50% 42%, rgba(184, 95, 50, 0.14), rgba(255, 250, 242, 0) 62%),
      #fff7eb;
  }

  .pch-product-card__media img {
    width: 100%;
    height: 100%;
    max-height: 148px;
    object-fit: contain;
    padding: 10px;
  }

  .pch-product-card__body {
    min-width: 0;
    padding: 13px 13px 12px;
  }

  .pch-product-card__body span,
  .pch-product-card__body strong,
  .pch-product-card__body small {
    display: block;
  }

  .pch-product-card__body span {
    margin-bottom: 6px;
    color: var(--pch-clay, #b85f32);
    font-size: 10px;
    font-weight: 850;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .pch-product-card__body strong {
    display: -webkit-box;
    overflow: hidden;
    color: var(--pch-ink, #171512);
    font-size: 14px;
    font-weight: 780;
    line-height: 1.2;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
  }

  .pch-product-card__body p {
    display: -webkit-box;
    overflow: hidden;
    margin: 7px 0 8px;
    color: var(--pch-muted, #5f574e);
    font-size: 12px;
    line-height: 1.35;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 3;
  }

  .pch-product-card__body small {
    color: var(--pch-ink, #171512);
    font-size: 12px;
    font-weight: 800;
  }

  .pch-switch__inner {
    display: grid;
    grid-template-columns: minmax(220px, 0.7fr) minmax(0, 1.3fr);
    gap: 18px;
    align-items: center;
  }

  .pch-switch h2 {
    margin: 0;
    color: var(--pch-ink, #171512);
    font-family: var(--prime-font-display, var(--prime-font-serif, Georgia, serif));
    font-size: clamp(24px, 3vw, 34px);
    line-height: 1.05;
    letter-spacing: 0;
  }

  .pch-switch__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 10px;
  }

  .pch-switch__grid article {
    padding: 16px;
    border: 1px solid rgba(55, 43, 31, 0.12);
    border-radius: 8px;
    background: #fffaf2;
  }

  .pch-switch__grid strong,
  .pch-switch__grid span {
    display: block;
  }

  .pch-switch__grid strong {
    color: var(--pch-ink, #171512);
    font-size: 14px;
    font-weight: 820;
  }

  .pch-switch__grid span {
    margin-top: 5px;
    color: var(--pch-muted, #5f574e);
    font-size: 12px;
    line-height: 1.45;
  }

  .pch-hero__pills span {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 8px 13px;
    border-radius: 999px;
    border: 1px solid rgba(55, 43, 31, 0.12);
    background: #ffffff;
    color: var(--pch-ink, #171512);
    font-size: 12px;
    font-weight: 600;
    line-height: 1;
  }

  .pch-hero__pills svg {
    flex-shrink: 0;
    opacity: 0.85;
  }

  /* ═══════════════════════════════════════════════════════════════
     TRUST BAR
  ════════════════════════════════════════════════════════════════ */
  .pch-trust-bar {
    background: #fff;
    border-bottom: 1px solid var(--pch-line, rgba(55, 43, 31, 0.13));
    padding: 10px 0;
  }

  .pch-trust-bar__inner {
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 0;
  }

  .pch-trust-bar__item {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 6px 16px;
    color: var(--pch-ink, #171512);
    font-size: 13px;
    font-weight: 600;
  }

  .pch-trust-bar__item svg {
    color: var(--pch-clay, #b85f32);
    flex-shrink: 0;
  }

  .pch-trust-bar__divider {
    width: 1px;
    height: 20px;
    background: var(--pch-line, rgba(55, 43, 31, 0.13));
    flex-shrink: 0;
  }

  /* ═══════════════════════════════════════════════════════════════
     SHOP BY NEED INTRO
  ════════════════════════════════════════════════════════════════ */
  .pch-need-intro {
    padding: 34px 0;
    background: #ffffff;
    border-bottom: 1px solid rgba(55, 43, 31, 0.1);
    font-family: var(--prime-font-sans, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif);
  }

  .pch-need-intro * { box-sizing: border-box; }

  .pch-need-intro__inner {
    display: grid;
    grid-template-columns: minmax(240px, 0.8fr) minmax(0, 1.2fr);
    gap: 22px;
    align-items: start;
  }

  .pch-need-intro__copy h2 {
    margin: 0;
    max-width: 460px;
    color: #171512;
    font-family: var(--prime-font-display, var(--prime-font-serif, Georgia, serif));
    font-size: clamp(26px, 3vw, 38px);
    line-height: 1.05;
    letter-spacing: 0;
  }

  .pch-need-intro__copy p:not(.pch-kicker) {
    margin: 12px 0 0;
    max-width: 520px;
    color: #5f574e;
    font-size: 15px;
    line-height: 1.6;
  }

  .pch-need-intro__cards {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 12px;
  }

  .pch-need-card {
    display: grid;
    align-content: start;
    min-height: 188px;
    padding: 18px;
    border: 1px solid rgba(55, 43, 31, 0.12);
    border-radius: 12px;
    background: #fffaf2;
    color: #171512;
    text-decoration: none;
    transition: border-color 0.2s ease, box-shadow 0.2s ease, transform 0.2s ease;
  }

  .pch-need-card:hover {
    border-color: rgba(184, 95, 50, 0.38);
    box-shadow: 0 14px 34px rgba(23, 59, 43, 0.08);
    transform: translateY(-1px);
  }

  .pch-need-card span {
    color: #b85f32;
    font-size: 11px;
    font-weight: 850;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .pch-need-card strong {
    display: block;
    margin-top: 14px;
    color: #171512;
    font-size: 20px;
    line-height: 1.12;
  }

  .pch-need-card p {
    margin: 10px 0 0;
    color: #5f574e;
    font-size: 13px;
    line-height: 1.55;
  }

  /* ═══════════════════════════════════════════════════════════════
     SIZE GUIDE SECTION
  ════════════════════════════════════════════════════════════════ */
  .pch-size-guide {
    padding: 56px 0 48px;
    background: #fffaf2;
    border-bottom: 1px solid rgba(55, 43, 31, 0.1);
    font-family: var(--prime-font-sans, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif);
  }

  .pch-size-guide * { box-sizing: border-box; }

  .pch-size-guide__header {
    max-width: 680px;
    margin: 0 0 36px;
  }

  .pch-size-guide__header h2 {
    margin: 0 0 12px;
    color: #171512;
    font-family: var(--prime-font-display, var(--prime-font-serif, Georgia, serif));
    font-size: clamp(26px, 3.2vw, 38px);
    font-weight: 760;
    line-height: 1.06;
    letter-spacing: 0;
  }

  .pch-size-guide__intro {
    margin: 0;
    color: #5f574e;
    font-size: 15px;
    line-height: 1.65;
  }

  .pch-size-guide__grid {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 14px;
  }

  .pch-size-card {
    background: #fff;
    border: 1px solid rgba(55, 43, 31, 0.12);
    border-radius: 12px;
    overflow: hidden;
    transition: box-shadow 0.2s ease, transform 0.2s ease;
  }

  .pch-size-card:hover {
    box-shadow: 0 12px 32px rgba(23, 59, 43, 0.1);
    transform: translateY(-2px);
  }

  .pch-size-card__badge {
    padding: 8px 16px;
    font-size: 11px;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: #fff;
  }

  .pch-size-card__badge--small  { background: #7a9e7e; }
  .pch-size-card__badge--medium { background: #5c8a6a; }
  .pch-size-card__badge--large  { background: #b85f32; }
  .pch-size-card__badge--xl     { background: #94451f; }
  .pch-size-card__badge--jumbo  { background: #171512; }

  .pch-size-card__body {
    padding: 16px;
  }

  .pch-size-card__weight {
    margin: 0 0 8px;
    color: #5f574e;
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .pch-size-card__body h3 {
    margin: 0 0 10px;
    color: #171512;
    font-size: 15px;
    font-weight: 760;
    line-height: 1.25;
  }

  .pch-size-card__body p {
    margin: 0 0 12px;
    color: #5f574e;
    font-size: 13px;
    line-height: 1.6;
  }

  .pch-size-card__tags {
    margin: 0;
    padding: 0;
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 5px;
  }

  .pch-size-card__tags li {
    display: flex;
    align-items: flex-start;
    gap: 6px;
    color: #3d6b4f;
    font-size: 12px;
    font-weight: 600;
    line-height: 1.35;
  }

  .pch-size-card__tags li::before {
    content: "✓";
    flex-shrink: 0;
    font-weight: 800;
    color: #3d6b4f;
  }

  /* ═══════════════════════════════════════════════════════════════
     CHEW GUIDE SECTION
  ════════════════════════════════════════════════════════════════ */
  .pch-chew-guide {
    padding: 56px 0 48px;
    background: #fff;
    border-bottom: 1px solid rgba(55, 43, 31, 0.1);
    font-family: var(--prime-font-sans, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif);
  }

  .pch-chew-guide * { box-sizing: border-box; }

  .pch-chew-guide__header {
    max-width: 640px;
    margin: 0 0 36px;
  }

  .pch-chew-guide__header h2 {
    margin: 0 0 10px;
    color: #171512;
    font-family: var(--prime-font-display, var(--prime-font-serif, Georgia, serif));
    font-size: clamp(26px, 3.2vw, 38px);
    font-weight: 760;
    line-height: 1.06;
  }

  .pch-chew-guide__sub {
    margin: 0;
    color: #5f574e;
    font-size: 15px;
    line-height: 1.6;
  }

  .pch-chew-guide__grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 18px;
  }

  .pch-chew-type {
    position: relative;
    padding: 24px;
    border: 1px solid rgba(55, 43, 31, 0.12);
    border-radius: 14px;
    background: #fffaf2;
  }

  .pch-chew-type--featured {
    border-color: rgba(184, 95, 50, 0.35);
    background: #fff7ee;
    box-shadow: 0 4px 20px rgba(184, 95, 50, 0.1);
  }

  .pch-chew-type__badge {
    display: inline-block;
    margin-bottom: 14px;
    padding: 4px 10px;
    border-radius: 999px;
    background: #b85f32;
    color: #fff;
    font-size: 10px;
    font-weight: 800;
    letter-spacing: 0.07em;
    text-transform: uppercase;
  }

  .pch-chew-type__icon {
    font-size: 28px;
    margin-bottom: 10px;
    line-height: 1;
  }

  .pch-chew-type h3 {
    margin: 0 0 10px;
    color: #171512;
    font-size: 18px;
    font-weight: 760;
    line-height: 1.2;
  }

  .pch-chew-type__desc {
    margin: 0 0 16px;
    color: #5f574e;
    font-size: 13px;
    line-height: 1.6;
  }

  .pch-chew-type__rec {
    margin-bottom: 16px;
    padding: 12px 14px;
    border-radius: 8px;
    background: #fff;
    border: 1px solid rgba(55, 43, 31, 0.1);
  }

  .pch-chew-type__label {
    display: block;
    margin-bottom: 4px;
    color: #b85f32;
    font-size: 10px;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .pch-chew-type__rec strong {
    color: #171512;
    font-size: 13px;
    font-weight: 760;
    line-height: 1.35;
  }

  .pch-chew-type__tips {
    margin: 0;
    padding: 0;
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .pch-chew-type__tips li {
    display: flex;
    align-items: flex-start;
    gap: 7px;
    color: #5f574e;
    font-size: 12px;
    line-height: 1.45;
  }

  .pch-chew-type__tips li::before {
    content: "→";
    flex-shrink: 0;
    color: #b85f32;
    font-weight: 700;
  }

  /* ═══════════════════════════════════════════════════════════════
     COMPARISON TABLE SECTION
  ════════════════════════════════════════════════════════════════ */
  .pch-comparison {
    padding: 56px 0 48px;
    background: #fffaf2;
    border-bottom: 1px solid rgba(55, 43, 31, 0.1);
    font-family: var(--prime-font-sans, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif);
  }

  .pch-comparison * { box-sizing: border-box; }

  .pch-comparison__header {
    max-width: 640px;
    margin: 0 0 32px;
  }

  .pch-comparison__header h2 {
    margin: 0;
    color: #171512;
    font-family: var(--prime-font-display, var(--prime-font-serif, Georgia, serif));
    font-size: clamp(26px, 3.2vw, 38px);
    font-weight: 760;
    line-height: 1.06;
  }

  .pch-comparison__table-wrap {
    overflow-x: auto;
    border-radius: 12px;
    border: 1px solid rgba(55, 43, 31, 0.12);
    background: #fff;
  }

  .pch-comparison__table {
    width: 100%;
    border-collapse: collapse;
    font-size: 13px;
    line-height: 1.4;
  }

  .pch-comparison__table thead th {
    padding: 14px 16px;
    text-align: left;
    font-size: 11px;
    font-weight: 800;
    letter-spacing: 0.07em;
    text-transform: uppercase;
    color: #5f574e;
    border-bottom: 2px solid rgba(55, 43, 31, 0.1);
    white-space: nowrap;
  }

  .pch-comparison__col--prime {
    background: rgba(184, 95, 50, 0.06);
  }

  .pch-comparison__table thead th.pch-comparison__col--prime {
    color: #b85f32;
    background: rgba(184, 95, 50, 0.1);
  }

  .pch-comparison__table tbody td {
    padding: 13px 16px;
    color: #171512;
    border-bottom: 1px solid rgba(55, 43, 31, 0.07);
    vertical-align: middle;
  }

  .pch-comparison__table tbody tr:last-child td {
    border-bottom: 0;
  }

  .pch-comparison__table tbody td:first-child {
    font-weight: 700;
    color: #5f574e;
    font-size: 12px;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    white-space: nowrap;
  }

  .pch-check  { color: #3d6b4f; font-weight: 800; margin-right: 4px; }
  .pch-cross  { color: #9a3412; font-weight: 800; margin-right: 4px; }
  .pch-neutral { color: #8a7e72; font-weight: 700; margin-right: 4px; }

  .pch-comparison__footnote {
    margin: 14px 0 0;
    color: #8a7e72;
    font-size: 11px;
    line-height: 1.5;
  }

  /* ═══════════════════════════════════════════════════════════════
     RESPONSIVE — Tablet
  ════════════════════════════════════════════════════════════════ */
  @media (max-width: 1024px) {
    .pch-size-guide__grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  @media (max-width: 860px) {
    .pch-hero__content {
      grid-template-columns: 1fr;
      gap: 24px;
    }

    .pch-product-panel__grid {
      display: flex;
      overflow-x: auto;
      padding: 0 2px 8px;
      scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch;
    }

    .pch-product-card {
      flex: 0 0 min(82vw, 360px);
      scroll-snap-align: start;
    }

    .pch-need-intro__inner {
      grid-template-columns: 1fr;
    }

    .pch-need-intro__cards {
      display: flex;
      grid-template-columns: none;
      gap: 12px;
      overflow-x: auto;
      padding: 0 2px 10px;
      scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch;
    }

    .pch-need-card {
      flex: 0 0 min(82vw, 320px);
      scroll-snap-align: start;
    }

    .pch-switch__inner {
      grid-template-columns: 1fr;
    }

    .pch-size-nav {
      display: flex;
      overflow-x: auto;
      padding-bottom: 4px;
      scroll-snap-type: x proximity;
    }

    .pch-size-nav a {
      min-width: 112px;
      scroll-snap-align: start;
    }

    .pch-trust-bar__divider {
      display: none;
    }

    .pch-trust-bar__inner {
      justify-content: flex-start;
      gap: 4px;
    }

    .pch-trust-bar__item {
      padding: 6px 12px;
      font-size: 12px;
    }

    .pch-chew-guide__grid {
      grid-template-columns: 1fr;
      gap: 14px;
    }

    .pch-size-guide__grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  /* ═══════════════════════════════════════════════════════════════
     RESPONSIVE — Mobile
  ════════════════════════════════════════════════════════════════ */
  @media (max-width: 560px) {
    .pch-shell {
      width: calc(100% - 24px);
    }

    .pch-hero__content {
      padding: 32px 0 26px;
    }

    .pch-product-panel__head {
      display: grid;
      justify-content: start;
      gap: 4px;
    }

    .pch-product-card {
      grid-template-columns: 96px minmax(0, 1fr);
      min-height: 138px;
    }

    .pch-product-card__media img {
      max-height: 138px;
    }

    .pch-hero__text h1 {
      font-size: 31px;
    }

    .pch-hero__sub {
      font-size: 15px;
    }

    .pch-trust-bar__inner {
      flex-direction: column;
      align-items: flex-start;
      gap: 8px;
    }

    .pch-trust-bar__item {
      padding: 4px 0;
    }

    .pch-switch__grid {
      grid-template-columns: 1fr;
    }

    .pch-size-guide__grid {
      grid-template-columns: 1fr;
    }

    .pch-size-guide,
    .pch-need-intro,
    .pch-chew-guide,
    .pch-comparison {
      padding: 36px 0 32px;
    }
  }
</style>

{% schema %}
{
  "name": "Prime Pet Food hero",
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow text",
      "default": "All Prime Pet Food chews"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading (leave blank to use collection title)",
      "info": "Defaults to collection title if empty"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading (leave blank to use collection description)",
      "info": "Defaults to collection description if empty"
    }
  ],
  "presets": [
    {
      "name": "Prime Pet Food hero"
    }
  ]
}
{% endschema %}
